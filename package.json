{
  "name": "chinese-signal-bot-server",
  "version": "5.1.0",
  "description": "Chinese Signal Bot — Broker Management System with Quotex Auto-Login",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "postinstall": "node -e \"try{require('child_process').execSync('npx --yes puppeteer browsers install chrome',{stdio:'inherit'})}catch(e){console.warn('Puppeteer Chrome install skipped:',e.message)}\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
  "imapflow": "^1.0.0"
},
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "puppeteer": "^22.0.0"
  }
}

